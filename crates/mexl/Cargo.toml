[package]
name = "mexl"
version = "0.2.0"
edition = "2024"

[features]
default = []
serde = ["dep:serde", "dep:serde_json"]

[dependencies]
serde = { version = "1.0", optional = true }
serde_json = { version = "1.0", optional = true }
thiserror = "2.0.18"